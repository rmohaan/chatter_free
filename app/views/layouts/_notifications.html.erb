<% flash.each do |message_type, message_content|  %>
  <div class="ui <%= message_type == "notice" ? "success" : "error" %> message transition container">
    <i class="close icon"></i>
    <div class="header">
      <%= message_content %>
    </div>
</div>
<% end %>